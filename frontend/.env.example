# Base URL of the backend API. This is baked into the browser bundle, so it must
# be reachable from the user's browser (the host), not from inside the docker network.
REACT_APP_API=http://localhost:8000/api
